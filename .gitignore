# Node dependencies (Do not commit)
frontend/node_modules/

# Python cache (Do not commit)
__pycache__/
.pytest_cache/

# Large data files (too large for GitHub — store locally or via LFS)
dataset.parquet
*.parquet
*.csv.gz
*.csv

# Keep frontend/dist since Render will serve it directly
!frontend/dist/
