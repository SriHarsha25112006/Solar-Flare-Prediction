# Node dependencies (Do not commit)
frontend/node_modules/

# Python cache (Do not commit)
__pycache__/
.pytest_cache/

# Large raw data files (too large for GitHub — local only)
dataset.parquet

# Keep model weights, data cache, and frontend/dist since Render will serve them directly
!models/*.pkl
!data_pipeline/cache/*.parquet
!frontend/dist/
