# Node dependencies (Do not commit)
frontend/node_modules/

# Python cache (Do not commit)
__pycache__/
.pytest_cache/

# Large raw data files (too large for GitHub — local only)
dataset.parquet
*.parquet
*.pkl

# Keep frontend/dist since Render will serve it directly
!frontend/dist/
